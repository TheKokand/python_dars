# Uchta son qabul qilib, ulardan eng kattasini qaytaruvchi funksiya yozing
def kottasini_top(bir,ikki,uc):
  max=bir
  if max<ikki:
    max=ikki
  elif max<uc:
    max=uc
  return max

x=kottasini_top(45,-985,27)
print(x)
